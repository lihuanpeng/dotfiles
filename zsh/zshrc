# Path to your oh-my-zsh installation.
export ZSH=~/.oh-my-zsh

# Set name of the theme to load. Look in ~/.oh-my-zsh/themes/
ZSH_THEME="my"

# Used plugins
plugins=(git osx)

# User configuration
PATH="/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin"
MANPATH="/usr/local/man:${MANPATH}"

# bootstrap zsh
source ${ZSH}/oh-my-zsh.sh

# set language environment
export LANG=zh_CN.UTF-8
export LANGUAGE=zh_CN.UTF-8

# set default editor
export EDITOR='vim'

# Compilation flags
export ARCHFLAGS="-arch x86_64"

export LINODE="139.162.125.45"
export WORKSTATION="192.168.199.190"

# Set personal aliases, overriding those provided by oh-my-zsh libs,
# plugins, and themes.
alias ll="ls -lhGF"
alias la="ls -lahGF"
alias cp="cp -i"
alias clonesite="wget -c -r -np -k -L -p"

# Diffrent system has diffrent configure
# for macbook-pro with osx
if [ $(uname) = "Darwin" ]; then
    # for osx
    alias wwwroot="cd /Library/WebServer/Documents"
    alias cleanqq="rm -rf /Users/lihuanpeng/Library/Containers/com.tencent.qq/Data/Library/Application\ Support/QQ/2705313595/Image"
    # those two lines for CUDA environment
    CUDA_PATH="/Developer/NVIDIA/CUDA-8.0"
    export PATH="${CUDA_PATH}/bin:${PATH}"
    export LD_LIBRARY_PATH="${CUDA_PATH}/lib:${LD_LIBRARY_PATH}"

    # for Qt env
    export QTDIR="/Applications/Qt5.9.x/5.9/clang_64"
    export PATH="${QTDIR}/bin:${PATH}"
    export LD_LIBRARY_PATH="${QTDIR}/lib:${LD_LIBRARY_PATH}"
fi


if [ $(uname) = "Linux" ]; then
    # for workstation with debian.
    export LD_LIBRARY_PATH="/lib:/lib64:/lib32:/usr/local/lib:/usr/local/lib64:/usr/local/lib32"

    # alias
    alias i3config="vim ~/.i3/config"

    # those two lines for CUDA environment
    CUDA_PATH="/usr/local/cuda"
    export PATH="${PATH}:${CUDA_PATH}/bin"
    export LD_LIBRARY_PATH="${LD_LIBRARY_PATH}:${CUDA_PATH}/lib64"

    # Qt env
    export QT_SELECT="Qt5.9"
    export QTDIR="/opt/Qt5.9.x/5.9/gcc_64"
    export PATH="${PATH}:${QTDIR}/bin"
    export LD_LIBRARY_PATH="${LD_LIBRARY_PATH}:${QTDIR}/lib"

    # MATLAB Runtime
    export MLRT_PATH="/opt/MATLAB/MATLAB_Runtime/v92"
    #export LD_LIBRARY_PATH="${LD_LIBRARY_PATH}:${MLRT_PATH}/runtime/glnxa64:${MLRT_PATH}/bin/glnxa64:${MLRT_PATH}/sys/os/glnxa64"
    # for java
    export JAVA_HOME="/usr/lib/jvm/jdk1.8.0_121"
    export JRE_HOME="${JAVA_HOME}/jre"
    export CLASSPATH="${JAVA_HOME}/lib64:${JRE_HOME}/lib64"
    export PATH="${PATH}:${JAVA_HOME}/bin"
fi

# export environments
export REPO_URL='https://mirrors.tuna.tsinghua.edu.cn/git/git-repo/'

#end of file
